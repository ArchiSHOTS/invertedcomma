# ── Required ──────────────────────────────────────────────────────────────────

# Neon PostgreSQL — production branch connection string
# Neon dashboard → your project → Branches → main → Connection string
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# JWT signing secret — generate with: openssl rand -base64 48
JWT_SECRET="change-this-to-a-long-random-string-in-production"

# Google Gemini API key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# ── Google Sign-In (optional — email/password works without these) ───────────
# Create an OAuth 2.0 Client ID (Web application) at:
#   https://console.cloud.google.com/apis/credentials
# Both must be the SAME client ID. VITE_ is baked into the frontend at build time;
# the server one is used to verify the audience of incoming Google tokens.
GOOGLE_CLIENT_ID="xxxxxxxx.apps.googleusercontent.com"
VITE_GOOGLE_CLIENT_ID="xxxxxxxx.apps.googleusercontent.com"

# ── Admin account bootstrap ──────────────────────────────────────────────────
# The server ensures an admin account from these on startup. On first deploy it
# migrates the legacy seeded admin to this email + password. Keep the password
# OUT of source — set these only as Render env vars (and local .env for dev).
ADMIN_EMAIL="invertedcommahq@gmail.com"
ADMIN_PASSWORD="choose-a-strong-password"
# Break-glass: if locked out, set ADMIN_PASSWORD above to a new value, set this
# to "true", redeploy once (resets the admin password), then remove the flag.
# ADMIN_PASSWORD_RESET="true"

# ── Transactional email (Resend) ─────────────────────────────────────────────
# Optional — signup/login work without it, but no verification/welcome emails
# are sent. Get a key at https://resend.com (verify invertedcomma.com first).
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxx"
# Verified sender. Must be on a domain you've verified in Resend.
EMAIL_FROM="Inverted Comma <hello@invertedcomma.com>"
# Canonical site URL used to build verification links.
SITE_URL="https://www.invertedcomma.com"

# ── Cloudflare Turnstile (bot/spam protection, optional) ──────────────────────
# Without these, forms (subscribe, sign up, comments) work normally but skip
# bot verification. To enable:
#   1. Go to https://dash.cloudflare.com → Turnstile → Add a site.
#   2. Use widget mode "Managed" (or "Invisible"), add invertedcomma.com
#      (and your local dev hostname, e.g. localhost) as domains.
#   3. Copy the "Site Key" and "Secret Key" into the two vars below.
# VITE_ is baked into the frontend at build time and is safe to expose.
TURNSTILE_SECRET_KEY="0x4AAAAAAAxxxxxxxxxxxxxxxxxxxxxxxxx"
VITE_TURNSTILE_SITE_KEY="0x4AAAAAAAxxxxxxxxxxxxxxxxxxxxxxxxx"

# ── Set automatically by Render — do not add these manually ──────────────────
# NODE_ENV=production
# PORT=10000
